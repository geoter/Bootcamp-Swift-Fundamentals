import Foundation
/*:
 ## Welcome to Swift Playground

 In this playground, you will learn how to create and manipulate classes and structs in Swift.

 ### Task 1: Create a Friend class
 - Create a class named `Friend` with two properties: `name` (a constant `String`) and `friends` (a variable `Int`).

 ### Task 2: Create a Friends struct
 - Create a struct named `Friends` with one property: `friendsList` (an array of `Friend` objects).

 ### Task 3: Instantiate and manipulate objects
 - Instantiate a `Friends` object with an initial list containing one `Friend` object.
 - Add a new `Friend` object to the `friendsList`. (Compiler Error)
 - Increment the `friends` property of the first `Friend` object in the list.
 - Print the updated number of friends of the first `Friend` object.

 ### Note
 - Use the `append` method to add a new element to an array.
 - Use optional chaining to safely access properties of optional values.
 */
